#Networking Basics

#!/bin/bash

echo "Checks connectivity and DNS resolution : ping"
ping google.com #try in command prompt
echo "Finds the IP address behind a domain name : nslookup"
nslookup google.com #try in command prompt
echo "Makes a request to a website from the terminal : curl"
curl google.com #try in command prompt
